OLD
1. Start Flask backend:
cd backend > flask run

2. Start frontend:
cd frontend > npm start (if UnauthorizedAccess error: Set-ExecutionPolicy RemoteSigned -Scope Process -Force)

NEW
1. Start Flask backend:
cd backend > python run.py

2. Start frontend:
cd frontend > npm start (if UnauthorizedAccess error: Set-ExecutionPolicy RemoteSigned -Scope Process -Force)